name: Update Posts (dev-news)

permissions:
  contents: write

on:
  push:
    branches:
      - dev-news-setup
  workflow_dispatch:

jobs:
  trigger:
    uses: jurakovic/dev-links/.github/workflows/update_posts.yaml@master_new
