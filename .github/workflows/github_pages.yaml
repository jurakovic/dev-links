name: Deploy GitHub Pages

permissions:
  pages: write
  id-token: write

on:
  push:
    branches:
      - dev-news
  workflow_dispatch:

jobs:
  trigger:
    uses: jurakovic/dev-links/.github/workflows/github_pages.yaml@master
